# مكتبة الفيديو — النسخة الاحترافية

هذه النسخة تعالج المشكلة الأساسية: لا نعرض الصفحة وكأنها تحتوي كل المسلسلات بينما الموجود حلقة أو حلقتان فقط. الصفحة الآن تعرض فقط **الحلقات المتاحة فعلاً** وتوضح إذا كانت السلسلة غير مكتملة.

## 1. طريقة بناء مكتبة قوية

لا تضف فيديوهات عشوائية. استعمل هذا النظام:

1. ابحث عن قناة/Playlist رسمية أو موثوقة.
2. ادخل إلى `admin.html`.
3. افتح تبويب `🎬 مكتبة الفيديو`.
4. استخدم `استيراد Playlist كاملة`.
5. اكتب اسم السلسلة، التصنيف، الموسم، ولغة الصوت.
6. بعد الاستيراد، نزّل `videos-data.js`.
7. استبدل الملف في المشروع، ثم أعد النشر على Vercel.

## 2. إعداد YouTube API

للاستيراد الاحترافي من Playlist، أضف في Vercel Environment Variables:

```txt
YOUTUBE_API_KEY=YOUR_KEY_HERE
```

الملف `/api/youtube.js` يجلب:

- العنوان
- اسم القناة
- المدة
- الوصف
- هل الفيديو يقبل التضمين embeddable
- هل فيه captions

الفيديوهات التي لا تقبل التضمين يتم تجاهلها عند الاستيراد.

## 3. خيار اللغة مثل Netflix

الصفحة فيها الآن خياران داخل المشغل:

- لغة الصوت: العربية / الدارجة / التركية / الفرنسية
- الترجمة: بدون / العربية / الفرنسية / التركية

مهم جداً:

- تغيير **الصوت** لا يعمل إلا إذا أضفت نسخة فيديو مختلفة لنفس الحلقة.
- YouTube لا يعطي تحكماً كاملاً من خارج المشغل في تغيير Audio Track الداخلي.
- لذلك النظام يستعمل أكثر من YouTube ID لنفس الحلقة.

مثال داخل `videos-data.js`:

```js
{
  title: 'الحلقة 1',
  ytId: 'AR_VIDEO_ID',
  audio: {
    ar: { ytId: 'AR_VIDEO_ID', label: 'العربية' },
    ma: { ytId: 'DARIJA_VIDEO_ID', label: 'الدارجة' },
    tr: { ytId: 'TURKISH_VIDEO_ID', label: 'التركية' }
  },
  captions: { off: true, ar: true, fr: true }
}
```

## 4. الدارجة المغربية

لا تنشر دبلجة كاملة لمسلسل لا تملك حقوقه. الأفضل:

- ملخص الحلقة بالدارجة
- تعليق/تحليل بالدارجة
- فيديو قصير يشرح الأحداث
- محتوى IA أصلي بالدارجة

ثم تربطه كنسخة `ma` داخل نفس الحلقة إذا كان مناسباً.

## 5. كيف تجعل الصفحة جذابة للزائر؟

- لا تعرض سلسلة فيها حلقة واحدة كأنها كاملة.
- استورد Playlist كاملة أو على الأقل 10 حلقات قبل إبراز السلسلة.
- أظهر عدد الحلقات المتاحة فقط.
- ضع مصدر واضح لكل فيديو.
- استعمل صورة السلسلة الجيدة.
- أضف محتوى دارجة خاص بك كي لا يكون الموقع نسخة من YouTube فقط.
